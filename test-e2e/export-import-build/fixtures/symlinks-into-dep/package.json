{
  "name": "symlinks-into-dep",
  "version": "1.0.0",
  "license": "MIT",
  "esy": {
    "build": [
      "ln -s #{dep.bin / dep.name} #{self.bin / self.name}"
    ]
  },
  "dependencies": {
    "dep": "*"
  }
}
