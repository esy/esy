(jbuild_version 1)

(executable
 ((name esyi)
  (package esyi)
  (public_name esyi)
  (flags (:standard "-open" "EsyLib"))
  (preprocess (pps (lwt.ppx ppx_let ppx_driver)))
  (libraries (fetch solve cmdliner))))
