(jbuild_version 1)

(library
 ((name opam)
  (preprocess (pps (ppx_deriving_yojson testre-ppx ppx_driver)))
  (libraries (npm shared lwt str curl cudf yaml ppx_deriving_yojson.runtime yojson opam-file-format))))
