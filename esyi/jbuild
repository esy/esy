(jbuild_version 1)

(library
 ((name EsyInstaller)
  (preprocess (pps (
                    lwt.ppx ppx_let
                    ppx_deriving.std
                    ppx_deriving_yojson
                    ppxlib
                    ppx_inline_test)))
  (inline_tests)
  (flags (:standard (-w -39) "-open" "EsyLib"))
  (libraries (
              esy-lib
              str
              cudf
              yaml
              ppx_deriving_yojson.runtime
              yojson
              logs
              logs.lwt
              dose3.algo
              opam-file-format))))
