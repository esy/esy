(executable
  (name esyRewritePrefix)
  (public_name esy-rewrite-prefix)
  (package esy-build-package)
  (preprocess (pps ppx_let))
  (libraries
            EsyBuildPackage
            fastreplacestring
            cmdliner
            bos
            )
  )

