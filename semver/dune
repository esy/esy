(library
  (name Semver)
  (inline_tests)
  (preprocess (pps
                  ppxlib
                  ppx_expect
                  ppx_inline_test
                  ))
  )

(ocamllex (modules Lexer))
(menhir
  (flags "--explain" "--dump")
  (modules Parser)
  )
